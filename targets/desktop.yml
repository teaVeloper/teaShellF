---
meta: # optional, ignored by tooling
  date: "2026-01-15"
  version: 1
  tags: ["base"]

defaults:
  repo: "tearoom"
  required: false

links:
  # Qtile (optional)
  - src: "qtile"
    dest: "$XDG_CONFIG_HOME/qtile"
    aliases: ["qtile", "de"]
