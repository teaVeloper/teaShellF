---
meta: # optional, ignored by tooling
  date: "2026-01-15"
  version: 1
  tags: ["base"]

defaults:
  repo: "teacup.nvim"
  required: false

links:
  # Neovim distro
  - src: "config"
    dest: "$XDG_CONFIG_HOME/nvim"
    aliases: ["nvim"]
