# ~/.bashrc
set -o vi


# Only run for interactive shells
case "$-" in
  *i*) ;;
  *) return ;;
esac

# Ensure base env is present even in interactive non-login bash
[ -f "$HOME/.profile" ] && . "$HOME/.profile"

# (Optional) a couple of convenience things; keep it sparse.
alias ll='ls -lah'
